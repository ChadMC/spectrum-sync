{
  "name": "spectrum-sync",
  "version": "1.0.0",
  "description": "TV-hosted, phone-controlled cooperative spectrum guessing game",
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm start",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build"
  },
  "keywords": [
    "game",
    "multiplayer",
    "party",
    "websocket"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
